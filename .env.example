# SIP/PRI Migration Tool - Environment Configuration
# Copy this file to .env and update with your actual values

# ===== DATABASE CONFIGURATION =====
# PostgreSQL connection settings for Metaswitch ShadowDB
# These should be READ-ONLY credentials for security

# Database host/IP address
DB_HOST=10.100.30.60

# Database port (default PostgreSQL port)
DB_PORT=5432

# Database name
DB_NAME=shadow_config_db

# Database username (should have READ-ONLY access)
DB_USER=shadowconfigread

# Database password
DB_PASSWORD=your_password_here

# NetSapiens API Configuration
# Base URL should include the /ns-api/v2/ path segment
NS_API_BASE_URL=https://portal.example.com/ns-api/v2/
NS_API_KEY=your_api_key_here
NS_SIPBX_SERVER=core1
NS_CORE_SERVER=core1-ord.grid4voice.ucaas.tech
ALLOW_DOMAIN_CREATE=false

# Adtran SSH credentials (read-only automation account)
ADTRAN_SSH_USER=adtran_readonly
ADTRAN_SSH_PASS=your_password_here
ADTRAN_ENABLE_PASS=your_enable_password_here
ADTRAN_TEST_IP=8.2.147.30
TEST_LIVE_ADTRAN=false
ALLOW_ADTRAN_APPLY=false

# Firmware gating defaults
MINIMUM_OS_MAJOR=13
RECOMMENDED_OS_VERSION=R13.12.0.E
STRICT_OS_GATING=false

# UI / feature toggles
NEXT_PUBLIC_ENABLE_AUTOMATION=false

# ===== APPLICATION CONFIGURATION =====
# Application port (default: 3000)
PORT=3000

# Node environment (production/development)
NODE_ENV=production

# ===== SECURITY NOTES =====
# 1. Ensure DB_USER has only SELECT permissions
# 2. Use strong passwords
# 3. Consider using environment-specific credentials
# 4. Never commit the actual .env file to version control
# 5. For production, consider using Docker secrets or similar

# ===== DATABASE TABLES ACCESSED =====
# The application requires READ access to:
# - meta_pbx_line (for PBX directory numbers)
# - meta_pbx_directinwardcalling (for DID ranges)

# ===== NETWORK REQUIREMENTS =====
# The application needs network access to:
# - PostgreSQL database on specified host/port
# - No outbound internet access required (internal tool)
